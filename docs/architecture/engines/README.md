# Engines

Execution engines that operate over the regulatory graph. Each subfolder documents a specific engine and its historical versions.

- `timeline-engine/` – time reasoning over statutory timelines.
- `scenario-engine/` – scenario and what-if evaluation atop the shared graph.
